'use strict';

/**
 * recruitment-application controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::recruitment-application.recruitment-application');
